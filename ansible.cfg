[defaults]
inventory = inventory.ini
interpreter_python = auto_silent
retry_files_enabled = False
stdout_callback = default

[privilege_escalation]
# playbook自体を sudo で起動する運用のため、Ansible内部ではbecomeしない。
become = False
